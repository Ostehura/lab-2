import { Comments } from 'src/comments/comments.entity';
import { Posts } from 'src/posts/posts.entity';
import { User } from 'src/users/users.entity';

export const entities = [Comments, Posts, User];
