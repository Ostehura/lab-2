<!DOCTYPE html>
<html>

<head>
    {{>head}}
</head>

<body>
    {{>header}}
    <div >
        

        <div class="container">
            <h1 class="text-center">{{post.title}}</h1>
            <h6 class="text-right">By {{post.author.username}}</h6>
            <p class="text-justify">{{post.description}}</p>

        </div>
        <div class="container">
            <div class="row d-flex justify-content-center">
                <img src="https://picsum.photos/600" width="600px" height="600px" />
            </div>
        </div>

        <div class="container py-5">
            <div class="row d-flex justify-content-center">
                <div class="col-md-12 col-lg-10">
                    {{#checkArray comments}}
                        <h1 class="display-4">Comments:</h1>
                        {{#each comments}}
                            {{>comment}}
                        {{/each}}
                    {{else}}
                        <h1 class="display-4">No comments here</h1>
                    {{/checkArray}}
                </div>
            </div>
        </div>
    </div>
    {{>footer}}
</body>

</html>